# OpenClaw 多 Agent 使用指南

## ✅ 配置完成

所有 agents 已正确配置并设置身份：

```
✅ main-agent    → 🤖 贾维斯 (Jarvis)
✅ content-agent → ✍️ 内容创作助手
✅ tech-agent    → 💻 技术开发助手
✅ ainews-agent  → 📰 AI资讯助手
```

## 📱 在飞书中使用

### 方法 1：切换 Agent（推荐）

在每个群组中发送命令切换 agent：

```
助理群：/agent main-agent
内容创作群：/agent content-agent
技术开发群：/agent tech-agent
AI资讯群：/agent ainews-agent
```

**优点**：
- 切换完整的 agent（包括 workspace、tools、身份）
- 会话会记住选择
- 每个 agent 有独立的上下文

### 方法 2：切换模型

如果只需要不同的模型，可以使用：

```
助理群：/model local-antigravity/claude-opus-4-6-thinking
内容创作群：/model local-antigravity/claude-sonnet-4-5
技术开发群：/model local-antigravity/claude-sonnet-4-5-thinking
AI资讯群：/model local-antigravity/gemini-2.5-flash
```

## 🔍 验证 Agent

发送命令查看当前使用的 agent：

```
/status
```

应该显示：
- Agent ID
- Agent 名称和 emoji
- 使用的模型
- Workspace 路径

## 📋 可用命令

```bash
/agent <agent-id>     # 切换 agent
/model <model-id>     # 切换模型
/status               # 查看当前状态
/reset                # 重置会话
/help                 # 查看帮助
```

## 🎯 Agent 特点

### 🤖 main-agent (贾维斯)
- **模型**：Claude Opus 4.6 Thinking
- **特点**：最强推理能力，适合复杂任务
- **适用**：综合任务、决策分析、复杂问题

### ✍️ content-agent (内容创作助手)
- **模型**：Claude Sonnet 4.5
- **特点**：平衡性能和成本，擅长创作
- **适用**：文章撰写、内容创作、文案策划

### 💻 tech-agent (技术开发助手)
- **模型**：Claude Sonnet 4.5 Thinking
- **特点**：带推理的技术助手
- **适用**：代码开发、技术问题、架构设计

### 📰 ainews-agent (AI资讯助手)
- **模型**：Gemini 2.5 Flash
- **特点**：快速响应，成本低
- **适用**：资讯整理、快速查询、日常对话

## ⚠️ 注意事项

1. **首次使用需要切换**
   - 每个群组首次使用时需要手动切换 agent
   - 切换后会话会记住选择

2. **重置会话会恢复默认**
   - 使用 `/reset` 后会恢复到 main-agent
   - 需要重新切换

3. **不同群组独立**
   - 每个群组可以使用不同的 agent
   - 互不影响

## 🔧 故障排查

### 问题：提示 "agent 未配置"

**解决方案**：
```bash
# 1. 检查 agent 列表
openclaw agents list

# 2. 重启网关
openclaw gateway restart

# 3. 在群组中重新尝试
/agent tech-agent
```

### 问题：切换后还是使用旧 agent

**解决方案**：
```bash
# 1. 重置会话
/reset

# 2. 重新切换
/agent tech-agent

# 3. 验证
/status
```

### 问题：想恢复默认 agent

**解决方案**：
```bash
/agent main-agent
```

## 📊 使用建议

### 场景 1：日常工作
- 使用 main-agent（贾维斯）
- 综合能力最强

### 场景 2：内容创作
- 使用 content-agent
- 专注创作，成本适中

### 场景 3：技术开发
- 使用 tech-agent
- 带推理能力，适合复杂代码

### 场景 4：快速查询
- 使用 ainews-agent
- 响应快，成本低

## 🎉 开始使用

1. **在助理群中**：
   ```
   /agent main-agent
   ```

2. **在内容创作群中**：
   ```
   /agent content-agent
   ```

3. **在技术开发群中**：
   ```
   /agent tech-agent
   ```

4. **在 AI 资讯群中**：
   ```
   /agent ainews-agent
   ```

5. **发送测试消息验证**

## 📞 获取帮助

如果遇到问题：

1. 查看日志：
   ```bash
   openclaw logs --follow
   ```

2. 检查配置：
   ```bash
   openclaw doctor
   ```

3. 查看 agents：
   ```bash
   openclaw agents list
   ```

---

**配置完成！现在可以在不同的飞书群组中使用不同的 agent 了！** 🎉
